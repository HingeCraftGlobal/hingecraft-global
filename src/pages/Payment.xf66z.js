// HingeCraft Global - SEO Optimized
// Payment | Secure Transaction Processing
// Generated: December 6, 2025

import wixSeo from 'wix-seo';

$w.onReady(function () {
    // Set page title
    wixSeo.setTitle("Payment | Secure Transaction Processing");
    
    // Set meta tags
    wixSeo.setMetaTags([
        { name: "description", content: "Complete your payment securely with HingeCraft. We accept all major payment methods with encrypted, PCI-compliant processing." },
        { name: "keywords", content: "payment, secure transaction, payment processing, credit card, online payment" },
        { name: "robots", content: "index, follow" },
        { property: "og:title", content: "Payment | Secure Transaction Processing" },
        { property: "og:description", content: "Complete your payment securely with HingeCraft. We accept all major payment methods with encrypted, PCI-compliant processing." },
        { property: "og:type", content: "website" },
        { property: "og:site_name", content: "HingeCraft Global" },
        { property: "twitter:card", content: "summary_large_image" },
        { property: "twitter:title", content: "Payment | Secure Transaction Processing" },
        { property: "twitter:description", content: "Complete your payment securely with HingeCraft. We accept all major payment methods with encrypted, PCI-compliant processing." }
    ]);
    
    // Set structured data (JSON-LD)
    wixSeo.setStructuredData([
        {
        "@context": "https://schema.org",
        "@type": "WebPage",
        "name": "Payment | Secure Transaction Processing",
        "description": "Complete your payment securely with HingeCraft. We accept all major payment methods with encrypted, PCI-compliant processing.",
        "url": "https://www.hingecraft.com/payment.xf66z",
        "inLanguage": "en-US",
        "dateModified": "2025-12-06",
        "publisher": {
                "@type": "Organization",
                "name": "HingeCraft Global",
                "url": "https://www.hingecraft.com",
                "logo": {
                        "@type": "ImageObject",
                        "url": "https://www.hingecraft.com/logo.png"
                }
        }
},
        {
        "@type": "BreadcrumbList",
        "itemListElement": [
                {
                        "@type": "ListItem",
                        "position": 1,
                        "name": "Home",
                        "item": "https://www.hingecraft.com"
                },
                {
                        "@type": "ListItem",
                        "position": 2,
                        "name": "Payment",
                        "item": "https://www.hingecraft.com/payment.xf66z"
                }
        ]
}
    ]);
});


// Original page code below
// Comprehensive SEO Optimization - Payment
// JSON-LD Schema.org | 100+ Keywords | Competitive Optimization

$w.onReady(function () {
    if (typeof document !== 'undefined') {
        // Page Title
        document.title = 'Payment | HingeCraft Global';
        
        // Meta Description
        let metaDesc = document.querySelector('meta[name="description"]');
        if (!metaDesc) {
            metaDesc = document.createElement('meta');
            metaDesc.setAttribute('name', 'description');
            document.head.appendChild(metaDesc);
        }
        metaDesc.setAttribute('content', 'Payment - HingeCraft Global. Comprehensive information, resources, and services.');
        
        // Meta Keywords
        let metaKeywords = document.querySelector('meta[name="keywords"]');
        if (!metaKeywords) {
            metaKeywords = document.createElement('meta');
            metaKeywords.setAttribute('name', 'keywords');
            document.head.appendChild(metaKeywords);
        }
        metaKeywords.setAttribute('content', 'hingecraft, hingecraft global, resilient design');
        
        // Open Graph Tags
        const ogTags = {
            'og:title': 'Payment | HingeCraft Global',
            'og:description': 'Payment - HingeCraft Global. Comprehensive information, resources, and services.',
            'og:image': 'https://hingecraft-global.ai/og-image.jpg',
            'og:url': 'https://hingecraft-global.ai/payment',
            'og:type': 'website',
            'og:site_name': 'HingeCraft Global'
        };
        
        Object.keys(ogTags).forEach(prop => {
            let ogMeta = document.querySelector(`meta[property="${prop}"]`);
            if (!ogMeta) {
                ogMeta = document.createElement('meta');
                ogMeta.setAttribute('property', prop);
                document.head.appendChild(ogMeta);
            }
            ogMeta.setAttribute('content', ogTags[prop]);
        });
        
        // Twitter Card Tags
        const twitterTags = {
            'twitter:card': 'summary_large_image',
            'twitter:title': 'Payment | HingeCraft Global',
            'twitter:description': 'Payment - HingeCraft Global. Comprehensive information, resources, and services.',
            'twitter:image': 'https://hingecraft-global.ai/og-image.jpg'
        };
        
        Object.keys(twitterTags).forEach(name => {
            let twitterMeta = document.querySelector(`meta[name="${name}"]`);
            if (!twitterMeta) {
                twitterMeta = document.createElement('meta');
                twitterMeta.setAttribute('name', name);
                document.head.appendChild(twitterMeta);
            }
            twitterMeta.setAttribute('content', twitterTags[name]);
        });
        
        // Canonical URL
        let canonical = document.querySelector('link[rel="canonical"]');
        if (!canonical) {
            canonical = document.createElement('link');
            canonical.setAttribute('rel', 'canonical');
            document.head.appendChild(canonical);
        }
        canonical.setAttribute('href', 'https://hingecraft-global.ai/payment');
        
        // Robots Meta
        let robots = document.querySelector('meta[name="robots"]');
        if (!robots) {
            robots = document.createElement('meta');
            robots.setAttribute('name', 'robots');
            document.head.appendChild(robots);
        }
        robots.setAttribute('content', 'index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1');
        
        // Additional Meta Tags
        const additionalTags = {
            'author': 'HingeCraft Global',
            'language': 'en-US',
            'revisit-after': '7 days',
            'distribution': 'global',
            'rating': 'general'
        };
        
        Object.keys(additionalTags).forEach(name => {
            let meta = document.querySelector(`meta[name="${name}"]`);
            if (!meta) {
                meta = document.createElement('meta');
                meta.setAttribute('name', name);
                document.head.appendChild(meta);
            }
            meta.setAttribute('content', additionalTags[name]);
        });
        
        // Schema.org JSON-LD Structured Data
        let schemaScript = document.querySelector('script[type="application/ld+json"]');
        if (!schemaScript) {
            schemaScript = document.createElement('script');
            schemaScript.setAttribute('type', 'application/ld+json');
            document.head.appendChild(schemaScript);
        }
        schemaScript.textContent = `{
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Payment | HingeCraft Global",
  "description": "Payment - HingeCraft Global. Comprehensive information, resources, and services.",
  "url": "https://hingecraft-global.ai/payment",
  "publisher": {
    "@type": "Organization",
    "name": "HingeCraft Global",
    "url": "https://hingecraft-global.ai",
    "logo": "https://hingecraft-global.ai/logo.png"
  },
  "datePublished": "2025-12-05",
  "dateModified": "2025-12-05",
  "inLanguage": "en-US",
  "keywords": "hingecraft, hingecraft global, resilient design"
}`;
    }

});


$w.onReady(function () {
    if (typeof document !== 'undefined') {
        // Page Title
        document.title = 'Payment | HingeCraft Global';
        
        // Meta Description
        let metaDesc = document.querySelector('meta[name="description"]');
        if (!metaDesc) {
            metaDesc = document.createElement('meta');
            metaDesc.setAttribute('name', 'description');
            document.head.appendChild(metaDesc);
        }
        metaDesc.setAttribute('content', 'Payment - HingeCraft Global. Comprehensive information and resources.');
        
        // Meta Keywords
        let metaKeywords = document.querySelector('meta[name="keywords"]');
        if (!metaKeywords) {
            metaKeywords = document.createElement('meta');
            metaKeywords.setAttribute('name', 'keywords');
            document.head.appendChild(metaKeywords);
        }
        metaKeywords.setAttribute('content', 'hingecraft, hingecraft global');
        
        // Open Graph Tags
        const ogTags = {
            'og:title': 'Payment | HingeCraft Global',
            'og:description': 'Payment - HingeCraft Global. Comprehensive information and resources.',
            'og:image': 'https://hingecraft-global.ai/og-image.jpg',
            'og:url': 'https://hingecraft-global.ai/payment',
            'og:type': 'website',
            'og:site_name': 'HingeCraft Global'
        };
        
        Object.keys(ogTags).forEach(prop => {
            let ogMeta = document.querySelector(`meta[property="${prop}"]`);
            if (!ogMeta) {
                ogMeta = document.createElement('meta');
                ogMeta.setAttribute('property', prop);
                document.head.appendChild(ogMeta);
            }
            ogMeta.setAttribute('content', ogTags[prop]);
        });
        
        // Twitter Card Tags
        const twitterTags = {
            'twitter:card': 'summary_large_image',
            'twitter:title': 'Payment | HingeCraft Global',
            'twitter:description': 'Payment - HingeCraft Global. Comprehensive information and resources.',
            'twitter:image': 'https://hingecraft-global.ai/og-image.jpg'
        };
        
        Object.keys(twitterTags).forEach(name => {
            let twitterMeta = document.querySelector(`meta[name="${name}"]`);
            if (!twitterMeta) {
                twitterMeta = document.createElement('meta');
                twitterMeta.setAttribute('name', name);
                document.head.appendChild(twitterMeta);
            }
            twitterMeta.setAttribute('content', twitterTags[name]);
        });
        
        // Canonical URL
        let canonical = document.querySelector('link[rel="canonical"]');
        if (!canonical) {
            canonical = document.createElement('link');
            canonical.setAttribute('rel', 'canonical');
            document.head.appendChild(canonical);
        }
        canonical.setAttribute('href', 'https://hingecraft-global.ai/payment');
        
        // Robots Meta
        let robots = document.querySelector('meta[name="robots"]');
        if (!robots) {
            robots = document.createElement('meta');
            robots.setAttribute('name', 'robots');
            document.head.appendChild(robots);
        }
        robots.setAttribute('content', 'index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1');
        
        // Schema.org JSON-LD Structured Data
        let schemaScript = document.querySelector('script[type="application/ld+json"]');
        if (!schemaScript) {
            schemaScript = document.createElement('script');
            schemaScript.setAttribute('type', 'application/ld+json');
            document.head.appendChild(schemaScript);
        }
        schemaScript.textContent = `{
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Payment | HingeCraft Global",
  "description": "Payment - HingeCraft Global. Comprehensive information and resources.",
  "url": "https://hingecraft-global.ai/payment",
  "publisher": {
    "@type": "Organization",
    "name": "HingeCraft Global",
    "url": "https://hingecraft-global.ai",
    "logo": "https://hingecraft-global.ai/logo.png"
  },
  "datePublished": "2025-12-05",
  "dateModified": "2025-12-05",
  "inLanguage": "en-US",
  "keywords": "hingecraft, hingecraft global"
}`;
    }
    
    // Load HTML content for legal pages
    
});




$w.onReady(function () {
    if (typeof document !== 'undefined') {
        // Page Title
        document.title = 'Payment | HingeCraft Global';
        
        // Meta Description
        let metaDesc = document.querySelector('meta[name="description"]');
        if (!metaDesc) {
            metaDesc = document.createElement('meta');
            metaDesc.setAttribute('name', 'description');
            document.head.appendChild(metaDesc);
        }
        metaDesc.setAttribute('content', 'Payment - HingeCraft Global. Comprehensive information and resources.');
        
        // Meta Keywords
        let metaKeywords = document.querySelector('meta[name="keywords"]');
        if (!metaKeywords) {
            metaKeywords = document.createElement('meta');
            metaKeywords.setAttribute('name', 'keywords');
            document.head.appendChild(metaKeywords);
        }
        metaKeywords.setAttribute('content', 'hingecraft, hingecraft global');
        
        // Open Graph Tags
        const ogTags = {
            'og:title': 'Payment | HingeCraft Global',
            'og:description': 'Payment - HingeCraft Global. Comprehensive information and resources.',
            'og:image': 'https://hingecraft-global.ai/og-image.jpg',
            'og:url': 'https://hingecraft-global.ai/payment',
            'og:type': 'website',
            'og:site_name': 'HingeCraft Global'
        };
        
        Object.keys(ogTags).forEach(prop => {
            let ogMeta = document.querySelector(`meta[property="${prop}"]`);
            if (!ogMeta) {
                ogMeta = document.createElement('meta');
                ogMeta.setAttribute('property', prop);
                document.head.appendChild(ogMeta);
            }
            ogMeta.setAttribute('content', ogTags[prop]);
        });
        
        // Twitter Card Tags
        const twitterTags = {
            'twitter:card': 'summary_large_image',
            'twitter:title': 'Payment | HingeCraft Global',
            'twitter:description': 'Payment - HingeCraft Global. Comprehensive information and resources.',
            'twitter:image': 'https://hingecraft-global.ai/og-image.jpg'
        };
        
        Object.keys(twitterTags).forEach(name => {
            let twitterMeta = document.querySelector(`meta[name="${name}"]`);
            if (!twitterMeta) {
                twitterMeta = document.createElement('meta');
                twitterMeta.setAttribute('name', name);
                document.head.appendChild(twitterMeta);
            }
            twitterMeta.setAttribute('content', twitterTags[name]);
        });
        
        // Canonical URL
        let canonical = document.querySelector('link[rel="canonical"]');
        if (!canonical) {
            canonical = document.createElement('link');
            canonical.setAttribute('rel', 'canonical');
            document.head.appendChild(canonical);
        }
        canonical.setAttribute('href', 'https://hingecraft-global.ai/payment');
        
        // Robots Meta
        let robots = document.querySelector('meta[name="robots"]');
        if (!robots) {
            robots = document.createElement('meta');
            robots.setAttribute('name', 'robots');
            document.head.appendChild(robots);
        }
        robots.setAttribute('content', 'index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1');
        
        // Schema.org JSON-LD Structured Data
        let schemaScript = document.querySelector('script[type="application/ld+json"]');
        if (!schemaScript) {
            schemaScript = document.createElement('script');
            schemaScript.setAttribute('type', 'application/ld+json');
            document.head.appendChild(schemaScript);
        }
        schemaScript.textContent = `{
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "Payment | HingeCraft Global",
  "description": "Payment - HingeCraft Global. Comprehensive information and resources.",
  "url": "https://hingecraft-global.ai/payment",
  "publisher": {
    "@type": "Organization",
    "name": "HingeCraft Global",
    "url": "https://hingecraft-global.ai",
    "logo": "https://hingecraft-global.ai/logo.png"
  },
  "datePublished": "2025-12-05",
  "dateModified": "2025-12-05",
  "inLanguage": "en-US",
  "keywords": "hingecraft, hingecraft global"
}`;
    }
    
    // Load HTML content for legal pages
    
});




/**
 * HingeCraft Payment Page Integration - NO DATABASE VERSION
 * 
 * This version works WITHOUT external database connection
 * Flow: Payment Page → Charter Page → Checkout
 * 
 * FIXES:
 * - Works without external database
 * - Redirects to charter page immediately (before checkout)
 * - Updates contributions section on charter page
 * - Then proceeds to checkout
 */

(function() {
  'use strict';

  // Wix Preview may execute this code in a non-DOM sandbox. Guard against missing globals.
  const HAS_DOM = typeof document !== 'undefined';
  const HAS_WINDOW = typeof window !== 'undefined';

  // Configuration
  const CONFIG = {
    STORAGE_KEY: 'hingecraft_donation',
    SESSION_KEY: 'hingecraft_donation',
    CHARTER_PAGE_URL: '/charter', // UPDATE THIS to your actual charter page URL
    CHECKOUT_PAGE_URL: '/checkout' // UPDATE THIS if needed
  };

  let donationAmount = null;

  /**
   * Get donation amount from form
   */
  function getDonationAmount() {
    // If DOM is unavailable (e.g., preview SSR), skip DOM queries
    if (!HAS_DOM) return null;

    const selectors = [
      '#other-amount',
      '#otherAmount',
      '#customAmount',
      'input[name="otherAmount"]',
      'input[name="customAmount"]',
      'input[type="number"][placeholder*="Other"]',
      'input[type="number"][placeholder*="Custom"]',
      '.other-amount-input',
      '.custom-amount-input',
      '[data-amount="other"]',
      '[data-testid="other-amount"]'
    ];

    for (const selector of selectors) {
      try {
        const element = document.querySelector(selector);
        if (element && element.value) {
          const amount = parseFloat(element.value);
          if (!isNaN(amount) && amount > 0) {
            return amount;
          }
        }
      } catch (e) {
        // Continue
      }
    }

    // Check URL parameter
    if (HAS_WINDOW) {
      const urlParams = new URLSearchParams(window.location.search);
      const urlAmount = urlParams.get('donationAmount') || urlParams.get('amount');
      if (urlAmount) {
        const amount = parseFloat(urlAmount);
        if (!isNaN(amount) && amount > 0) {
          return amount;
        }
      }
    }

    return null;
  }

  /**
   * Store donation amount (local storage only - no database)
   */
  function storeDonationAmount(amount) {
    try {
      // Store in sessionStorage
      if (typeof sessionStorage !== 'undefined') {
        sessionStorage.setItem(CONFIG.SESSION_KEY, JSON.stringify({
          amount: amount,
          timestamp: new Date().toISOString(),
          source: 'payment_page'
        }));
      }

      // Store in Wix Storage (if available)
      if (typeof wixStorage !== 'undefined') {
        wixStorage.setItem(CONFIG.STORAGE_KEY, JSON.stringify({
          amount: amount,
          timestamp: new Date().toISOString(),
          source: 'payment_page'
        }));
      }

      console.log('✅ Donation amount stored:', amount);
      return true;
    } catch (error) {
      console.error('❌ Error storing donation amount:', error);
      return false;
    }
  }

  /**
   * Redirect to charter page (BEFORE checkout)
   */
  function redirectToCharterPage(amount) {
    const charterUrl = `${CONFIG.CHARTER_PAGE_URL}?donationAmount=${encodeURIComponent(amount)}&fromPayment=true`;
    console.log('✅ Redirecting to charter page:', charterUrl);
    
    // Use Wix location API if available
    if (typeof wixLocation !== 'undefined' && wixLocation.to) {
      wixLocation.to(charterUrl);
    } else if (HAS_WINDOW) {
      window.location.href = charterUrl;
    }
  }

  /**
   * Handle form submission
   * NEW FLOW: Redirect to charter page immediately (before checkout)
   * Flow: Payment Page → Charter Page → Checkout
   */
  function handleFormSubmit(event) {
    // Get donation amount
    const amount = getDonationAmount();
    
    if (amount && amount > 0) {
      donationAmount = amount;
      
      // Store amount locally
      storeDonationAmount(amount);
      
      console.log('💰 Donation amount captured:', amount);
      console.log('🔄 Redirecting to charter page (before checkout)');
      
      // PREVENT default form submission
      if (event && event.preventDefault) {
        event.preventDefault();
      }
      
      // Stop propagation
      if (event && event.stopPropagation) {
        event.stopPropagation();
      }
      
      // Redirect to charter page IMMEDIATELY (before checkout)
      redirectToCharterPage(amount);
      
      return false; // Prevent form submission
    }
    
    // If no "Other" amount, let normal flow continue (goes to checkout)
    console.log('ℹ️ No "Other" amount - continuing with normal checkout flow');
    return true; // Allow form submission
  }

  /**
   * Handle button click
   * Flow: Payment Page → Charter Page → Checkout
   */
  function handleButtonClick(event) {
    const amount = getDonationAmount();
    
    if (amount && amount > 0) {
      donationAmount = amount;
      
      // Store amount locally
      storeDonationAmount(amount);
      
      console.log('💰 Donation amount captured:', amount);
      console.log('🔄 Redirecting to charter page (before checkout)');
      
      // Prevent default button action
      if (event && event.preventDefault) {
        event.preventDefault();
      }
      
      // Stop propagation
      if (event && event.stopPropagation) {
        event.stopPropagation();
      }
      
      // Redirect to charter page IMMEDIATELY
      redirectToCharterPage(amount);
      
      return false;
    }
    
    // Let button work normally if no "Other" amount
    return true;
  }

  /**
   * Initialize payment page integration
   */
  function init() {
    console.log('🚀 HingeCraft Payment Page Integration initialized (NO DATABASE VERSION)');
    console.log('📋 Flow: Payment Page → Charter Page → Checkout');

    // If DOM is not available (e.g., Wix preview server-side), defer until client runtime
    if (!HAS_DOM) {
      console.log('ℹ️ DOM not available in this context. Skipping DOM bindings.');
      return;
    }

    // Method 1: Listen for form submission
    const forms = document.querySelectorAll('form');
    forms.forEach(form => {
      form.addEventListener('submit', function(e) {
        const result = handleFormSubmit(e);
        if (result === false) {
          e.preventDefault();
          e.stopPropagation();
          return false;
        }
      }, true); // Use capture phase
    });

    // Method 2: Listen for button clicks
    const buttonSelectors = [
      'button[type="submit"]',
      'button[data-testid="submit"]',
      '[data-submit="payment"]',
      '.submit-payment',
      '[data-testid="submit-payment"]',
      'button.wixui-button',
      'button[aria-label*="Submit"]',
      'button[aria-label*="Pay"]'
    ];

    buttonSelectors.forEach(selector => {
      try {
        const buttons = document.querySelectorAll(selector);
        buttons.forEach(button => {
          button.addEventListener('click', function(e) {
            const result = handleButtonClick(e);
            if (result === false) {
              e.preventDefault();
              e.stopPropagation();
              return false;
            }
          }, true); // Use capture phase
        });
      } catch (e) {
        // Continue
      }
    });

    // Method 3: Use Wix $w API if available
    if (typeof $w !== 'undefined' && $w.onReady) {
      $w.onReady(function() {
        try {
          const paymentForm = $w('#paymentForm') || $w('#checkoutForm');
          if (paymentForm && paymentForm.onSubmit) {
            paymentForm.onSubmit(function() {
              const amount = getDonationAmount();
              if (amount && amount > 0) {
                storeDonationAmount(amount);
                redirectToCharterPage(amount);
                return false; // Prevent form submission
              }
              return true; // Allow form submission
            });
          }
        } catch (e) {
          console.log('Wix $w API not available:', e);
        }
      });
    }
  }

  // Initialize
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }

  // Also initialize after delay for dynamic content
  setTimeout(init, 1000);

})();

